# 三級 CMOS 環形振盪器專案說明

## 專案概述

本專案實作一個使用 SkyWater 130nm PDK 的三級 CMOS 環形振盪器(3-Stage CMOS Ring Oscillator)。環形振盪器是由奇數個反相器串接而成的振盪電路,廣泛應用於時脈產生、頻率合成器、及製程監控等應用。

## 工作原理

### 基本概念
環形振盪器由奇數個反相器串聯組成一個回授環路。每個反相器都會產生延遲,當輸出回授至輸入時,系統會自然產生振盪。

### 振盪頻率公式
```
f = 1 / (2 × N × td)
```
其中:
- `f` = 振盪頻率
- `N` = 反相器級數(本專案為 3)
- `td` = 單一反相器的延遲時間

### 電路架構
```
in1 → [反相器1] → out1 → [反相器2] → out2 → [反相器3] → 回授至 in1
```

每個反相器由一個 NMOS 和一個 PMOS 電晶體組成:
- **NMOS**: W=0.42µm, L=0.15µm (下拉網路)
- **PMOS**: W=0.84µm, L=0.15µm (上拉網路,尺寸較大以平衡驅動能力)

## 檔案結構

```
Three-stage-CMOS-ring-oscillator/
├── ring_oscillator.cir         # 主要電路模擬檔案
├── vdd_sweep.cir               # 電壓掃描分析檔案
├── plot_vdd_freq.py            # 繪圖腳本
├── vdd_freq_results.csv        # 模擬結果數據
├── vdd_freq_plot.png           # 電壓-頻率關係圖
├── simulation.log              # 模擬日誌
└── 專案說明_繁體中文.md        # 本說明文件
```

## 模擬環境需求

### 必要工具

#### 1. ngspice (SPICE 電路模擬器)
```bash
sudo apt install ngspice
```
- 版本需求: ngspice-42 或更新版本
- 驗證安裝: `ngspice --version`

#### 2. SkyWater 130nm PDK (製程設計套件)
**⚠️ 這是最關鍵的依賴項目，沒有 PDK 無法執行模擬！**

**方法 1: 使用 volare (推薦)**
```bash
# 安裝 volare
pip3 install volare

# 下載並啟用 SkyWater 130nm PDK
volare enable --pdk sky130 44a43c23c81b45b8e774ae7a84899a5a778b6b0b

# PDK 會安裝到 ~/.volare/sky130/
```

**方法 2: 手動下載**
```bash
# 克隆 SkyWater PDK 倉庫
git clone https://github.com/google/skywater-pdk.git
cd skywater-pdk

# 下載子模組
git submodule update --init libraries/sky130_fd_pr/latest

# 建立符號連結到預期路徑
mkdir -p /home/jimmy/pdk
ln -s $(pwd)/libraries/sky130_fd_pr/latest /home/jimmy/pdk/sky130A
```

**方法 3: 修改電路檔案使用自己的 PDK 路徑**

如果你的 PDK 安裝在不同路徑，請修改以下檔案中的 `.lib` 路徑:
- `ring_oscillator.cir` (第 3 行)
- `vdd_sweep.cir` (第 3 行)

範例:
```spice
.lib /your/path/to/sky130A/libs.tech/ngspice/sky130.lib.spice tt
```

**驗證 PDK 安裝**
```bash
# 檢查關鍵檔案是否存在
ls /home/jimmy/pdk/sky130A/libs.tech/ngspice/sky130.lib.spice
```

#### 3. Python 3 繪圖套件
```bash
pip3 install --break-system-packages matplotlib pandas
```

#### 4. 中文字型 (解決圖表亂碼)
```bash
sudo apt install fonts-noto-cjk
```

**或者手動安裝 Noto Serif CJK JP:**
```bash
# Ubuntu/Debian
sudo apt install fonts-noto-cjk

# Fedora/RHEL
sudo dnf install google-noto-serif-cjk-ttc-fonts

# Arch Linux
sudo pacman -S noto-fonts-cjk
```

**驗證字型安裝:**
```bash
fc-list | grep "Noto Serif CJK"
```

**如果仍有亂碼問題:**
1. 清除 matplotlib 快取:
```bash
rm -rf ~/.cache/matplotlib
```

2. 修改 `plot_vdd_freq.py` 使用其他中文字型:
```python
# 可選的中文字型
plt.rcParams["font.family"] = "Noto Sans CJK TC"  # 繁體中文
# 或
plt.rcParams["font.family"] = "WenQuanYi Micro Hei"  # 文泉驛微米黑
```

## 如何執行模擬

### 1. 基本振盪器模擬
```bash
cd Three-stage-CMOS-ring-oscillator
ngspice -b ring_oscillator.cir
```

這將執行暫態分析並顯示:
- 在標準電壓(1.8V)下的振盪頻率
- 振盪週期

### 2. 電壓掃描分析
```bash
ngspice -b vdd_sweep.cir
```

這會對不同供應電壓(1.2V ~ 2.0V)進行掃描分析,並產生 `vdd_freq_results.csv`

### 3. 繪製結果圖表
```bash
python3 plot_vdd_freq.py
```

產生 `vdd_freq_plot.png`,顯示電壓與頻率的關係

## 模擬結果

### 標準電壓(1.8V)下的性能
- **振盪頻率**: 5.60 GHz
- **振盪週期**: 0.178 ns
- **功耗**: 約 0.3 mW

### 電壓掃描結果

| 供應電壓 (V) | 頻率 (GHz) | 週期 (ns) |
|------------|-----------|----------|
| 1.2        | 2.51      | 0.399    |
| 1.3        | 3.22      | 0.310    |
| 1.4        | 3.61      | 0.277    |
| 1.5        | 3.65      | 0.274    |
| 1.6        | 4.71      | 0.212    |
| 1.7        | 5.57      | 0.180    |
| **1.8**    | **5.60**  | **0.178**|
| 1.9        | 6.16      | 0.162    |
| 2.0        | 7.08      | 0.141    |

### 重要觀察
1. **電壓-頻率正相關**: 供應電壓越高,振盪頻率越快
2. **頻率範圍**: 2.51 GHz (1.2V) ~ 7.08 GHz (2.0V)
3. **靈敏度**: 在 1.5V-1.8V 之間有顯著的頻率提升
4. **符合理論**: 結果與 CMOS 反相器延遲理論一致

## 電路詳細說明

### 元件參數

#### 電晶體尺寸
- **NMOS** (sky130_fd_pr__nfet_01v8):
  - 通道長度 (L): 0.15 µm
  - 通道寬度 (W): 0.42 µm

- **PMOS** (sky130_fd_pr__pfet_01v8):
  - 通道長度 (L): 0.15 µm
  - 通道寬度 (W): 0.84 µm (為 NMOS 的 2 倍,補償較低的載子遷移率)

#### 供應電壓
- 標準 VDD: 1.8V
- 地端 (GND): 0V

#### 初始條件
- v(in1) = 0V
- v(out1) = 1.8V
- 這些初始條件幫助電路快速開始振盪

### 模擬參數
- **分析類型**: 暫態分析 (.tran)
- **時間步長**: 0.1 ns
- **總模擬時間**: 200 ns
- **製程角**: tt (typical-typical)

## 應用場景

1. **時脈產生器**: 為數位電路提供時脈訊號
2. **壓控振盪器 (VCO)**: 可透過改變供應電壓來調整頻率
3. **製程監控**: 監測晶片製程變異
4. **隨機數產生器**: 利用相位雜訊產生隨機性
5. **頻率合成器**: 作為 PLL 的一部分

## 疑難排解

### 常見問題

#### 1. ❌ SkyWater PDK 未找到
**錯誤訊息**:
```
Error: can't find model 'sky130_fd_pr__nfet_01v8'
或
SkyWater PDK 未找到
```

**原因**: 沒有安裝 SkyWater 130nm PDK，或路徑不正確

**解決方案**:
1. **檢查 PDK 是否已安裝**:
```bash
ls /home/jimmy/pdk/sky130A/libs.tech/ngspice/sky130.lib.spice
```

2. **如果檔案不存在，安裝 PDK**:
```bash
# 方法 A: 使用 volare (推薦)
pip3 install volare
volare enable --pdk sky130 44a43c23c81b45b8e774ae7a84899a5a778b6b0b

# 方法 B: 手動克隆
git clone https://github.com/google/skywater-pdk.git
cd skywater-pdk
git submodule update --init libraries/sky130_fd_pr/latest
```

3. **如果 PDK 在不同位置，修改電路檔案**:
   編輯 `ring_oscillator.cir` 和 `vdd_sweep.cir` 第 3 行，改為你的 PDK 路徑

**⚠️ 重要**: 沒有 PDK 絕對無法執行任何模擬！

#### 2. ❌ 圖表中文亂碼
**問題**: `vdd_freq_plot.png` 中文字顯示為方框或亂碼

**解決方案**:

**步驟 1: 安裝中文字型**
```bash
sudo apt install fonts-noto-cjk
```

**步驟 2: 清除 matplotlib 快取**
```bash
rm -rf ~/.cache/matplotlib
```

**步驟 3: 重新產生圖表**
```bash
python3 plot_vdd_freq.py
```

**步驟 4: 驗證字型是否安裝**
```bash
fc-list | grep "Noto Serif CJK"
```

**替代方案**: 如果 Noto Serif CJK JP 無法使用，修改 `plot_vdd_freq.py`:
```python
# 第 13 行改為:
plt.rcParams["font.family"] = "Noto Sans CJK TC"  # 或其他已安裝的中文字型
```

可用的中文字型:
- `Noto Sans CJK TC` (繁體中文)
- `Noto Sans CJK SC` (簡體中文)
- `WenQuanYi Micro Hei` (文泉驛微米黑)

#### 3. ❌ 模擬失敗: "can't find model"
**問題**: ngspice 找不到電晶體模型
**解決方案**:
- 確認 PDK 路徑正確
- 使用 `.lib` 而非 `.include` 載入模型
- 元件名稱前加 `X` (子電路呼叫)
- 檢查模型名稱: `sky130_fd_pr__nfet_01v8` (不是 `sky130_fd_pr__nfet_01v8__tt`)

#### 4. ❌ 電路不振盪
**問題**: 輸出保持穩定,沒有振盪
**解決方案**:
- 檢查初始條件設定 (`.ic v(in1)=0 v(out1)=1.8`)
- 確認反相器級數為奇數 (3 級)
- 增加模擬時間 (`.tran 0.1n 200n`)
- 檢查電晶體連接是否正確

#### 5. ❌ Python 缺少模組
**錯誤**: `ModuleNotFoundError: No module named 'matplotlib'`
**解決方案**:
```bash
pip3 install --break-system-packages matplotlib pandas
```

#### 6. ❌ run_all.sh 權限不足
**錯誤**: `Permission denied: ./run_all.sh`
**解決方案**:
```bash
chmod +x run_all.sh
./run_all.sh
```

## 延伸實驗建議

1. **改變級數**: 嘗試 5 級或 7 級振盪器,觀察頻率變化
2. **調整電晶體尺寸**: 改變 W/L 比例,觀察對頻率和功耗的影響
3. **製程角分析**: 測試 ff, ss, sf, fs 等不同製程角
4. **負載分析**: 加入電容負載,模擬實際應用情況
5. **功耗分析**: 計算在不同電壓下的平均功耗
6. **溫度掃描**: 分析溫度對振盪頻率的影響

## 技術參考

### SPICE 指令說明
- `.lib`: 載入 SPICE 模型庫
- `.ic`: 設定初始條件
- `.tran`: 暫態分析
- `.control/.endc`: 控制區塊
- `meas`: 量測指令

### SkyWater PDK 元件
- `sky130_fd_pr__nfet_01v8`: 1.8V NMOS 電晶體
- `sky130_fd_pr__pfet_01v8`: 1.8V PMOS 電晶體

## 參考資料

1. B. Razavi, "Design of Analog CMOS Integrated Circuits", McGraw-Hill
2. SkyWater PDK Documentation: https://skywater-pdk.readthedocs.io/
3. ngspice User Manual: http://ngspice.sourceforge.net/docs.html
4. Ring Oscillator Design: https://en.wikipedia.org/wiki/Ring_oscillator

## 作者與貢獻

- **原作者**: Mohamed Irfan
- **PDK**: SkyWater Technology Foundry & Google
- **本地化與優化**: 2025

## 授權

本專案使用 SkyWater PDK,遵循 Apache License 2.0

---

**最後更新**: 2025-10-05
**版本**: 1.0
