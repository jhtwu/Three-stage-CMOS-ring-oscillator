# 快速開始指南

## ⚠️ 前置需求

**在執行模擬之前，必須先安裝 SkyWater 130nm PDK！**

### 快速安裝 PDK
```bash
# 方法 1: 使用 volare (推薦)
pip3 install volare
volare enable --pdk sky130

# 方法 2: 如果 PDK 路徑不同，修改電路檔案中的路徑
# 編輯 ring_oscillator.cir 和 vdd_sweep.cir 第 3 行
```

### 安裝其他依賴
```bash
# ngspice
sudo apt install ngspice

# Python 繪圖套件
pip3 install --break-system-packages matplotlib pandas

# 中文字型 (避免圖表亂碼)
sudo apt install fonts-noto-cjk
```

## 一鍵執行

```bash
cd Three-stage-CMOS-ring-oscillator
chmod +x run_all.sh
./run_all.sh
```

這個腳本會自動:
1. ✅ 檢查所需工具 (ngspice, python3)
2. ✅ **驗證 SkyWater PDK 是否已安裝**
3. ✅ 執行環形振盪器模擬
4. ✅ 進行電壓掃描分析 (1.2V ~ 2.0V)
5. ✅ 產生電壓-頻率關係圖

## 預期結果

### 標準電壓 (1.8V) 性能
- **振盪頻率**: 5.60 GHz
- **振盪週期**: 0.178 ns

### 電壓範圍性能
- **最低**: 2.51 GHz @ 1.2V
- **最高**: 7.08 GHz @ 2.0V

## 產生的檔案

| 檔案 | 說明 |
|------|------|
| `vdd_freq_plot.png` | 📊 電壓-頻率關係圖 |
| `vdd_freq_results.csv` | 📁 數據表格 |
| `ring_oscillator.raw` | 📈 波形數據 |

## 結果驗證

### ✅ 模擬成功指標
1. 頻率隨電壓增加而上升
2. 週期隨電壓增加而下降
3. 在 1.8V 時頻率約 5-6 GHz
4. 圖形顯示平滑的曲線

### ❌ 可能的問題

#### PDK 未找到
```
錯誤: SkyWater PDK 未找到
```
**解決**: 安裝 PDK (見上方「前置需求」)

#### 中文亂碼
圖表中文顯示為方框
**解決**:
```bash
sudo apt install fonts-noto-cjk
rm -rf ~/.cache/matplotlib
python3 plot_vdd_freq.py
```

#### 其他問題
- **找不到模型**: 檢查 PDK 路徑是否正確
- **不振盪**: 確認反相器級數為奇數
- **頻率異常**: 檢查電晶體尺寸參數

## 進階操作

### 單獨執行基本模擬
```bash
ngspice -b ring_oscillator.cir
```

### 單獨執行電壓掃描
```bash
ngspice -b vdd_sweep.cir
```

### 重新產生圖表
```bash
python3 plot_vdd_freq.py
```

### 查看波形(互動模式)
```bash
ngspice ring_oscillator.cir
# 在 ngspice 提示符下輸入:
plot v(in1) v(out1) v(out2)
```

## 更多資訊

詳細說明請參考: `專案說明_繁體中文.md`

---
**提示**: 如果遇到問題,先執行 `./run_all.sh` 檢查工具是否正確安裝
